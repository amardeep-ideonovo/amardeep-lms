"use client";

// Moved to @lms/ui (docs/coding-standards.md D5; shipped as a per-app copy in
// #129). Re-exported so existing `@/lib/useModalA11y` imports stay stable.
export { useModalA11y } from "@lms/ui";
