# Control-plane runs standalone against provisioner state (mocked in lib/provisioner.ts
# until the fleet API exists). No instance API URL is required to render.
# Port convention: api 3000, admin 3001, web 3002, control-plane 3003.
