# Restore-drill log

A backup you've never restored is a wish, not a backup. Run
`deploy/scripts/restore-drill.sh` at least once per quarter (it appends a line
here automatically) and eyeball the row counts against production.

| Date | Result |
|---|---|
| _(no drills recorded yet — run `deploy/scripts/restore-drill.sh`)_ | |
